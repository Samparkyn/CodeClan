def grab_first( array)
  return array[0]
end

def index_at(array, index)
  #array = [1,2,3,4,5] *No need for this, because it gets the array that as already been passed in*
  return array[index] 
end

def last_index(array, last_index)
  return array[last_index]
end